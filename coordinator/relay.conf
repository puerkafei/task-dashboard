# relay.conf — 角色到 Agent 的映射配置
# Used by coordinator.sh relay subcommand for auto-handoff
# <!-- V5 v2026.05.28.2 -->
#
# Each section header is the role name used in status.json steps[].assignee
# [role_name]
#   agentId   = OpenClaw agent session ID
#   type      = native | acp  (native: direct CLI call; acp: requires spawn via caocao)

[曹操]
agentId = caocao
type = native

[诸葛亮]
agentId = zhugeliang
type = native

[司马懿]
agentId = simayi
type = native

[曹植]
agentId = caozhi
type = native

[貂蝉（OpenCode）]
agentId = opencode
type = acp

